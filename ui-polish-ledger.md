| Component | State | Scope | Transition decision | Polish changes | Verification | Score | Status | Next action |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `Toolbar` / `ToolbarSelect` (`src/ui/Toolbar.tsx`, `src/styles/masthead.css`) | default, hover, focus, open, closing, option hover, mobile, reduced motion | primitive, navigation, form | `05-menu-dropdown.md` installed because each select is a trigger-anchored dropdown surface | Added open/closing state orchestration tied to `--dropdown-close-dur`; tuned close to 260ms; added `.t-dropdown` scale/opacity transition, origin-aware transform, chevron rotation, option easing, 0.96 press scale, and reduced-motion guard | `npm test -- src/ui`; `npm run build`; in-app Browser `iab` on `http://127.0.0.1:5176` verified `.t-dropdown.is-open`, origin `top-right`, transition properties `transform, opacity`, 250ms open duration, rotated chevron, 6 options, Last week selection, cleanup to 0 visible menus | 92 pass | passing | None |
| `SessionCard` (`src/ui/SessionCard.tsx`, `src/styles/masthead.css`) | default, hover, active, keyboard focus, layout-change, attention/conflict/running/stalled, compact grid, mobile, reduced motion | composite, card | Manual FLIP layout transition fits compact/comfortable density changes because the board changes card positions and dimensions; `19-card-tilt.md` rejected as too heavy for dense operational cards | Added per-card `data-session-id`, per-card `view-transition-name`, height/min-height easing, transient `.is-layout-animating` transform promotion, and 0.96 active press scale | `npm test -- src/ui`; `npm run build`; in-app Browser `iab` on `http://127.0.0.1:5176` verified 3 cards animate on density toggle with `.is-layout-animating`, inline `transform 300ms cubic-bezier(0.22, 1, 0.36, 1)`, non-none computed transforms, then cleanup back to empty inline styles | 94 pass | passing | None |
| `SessionDetailModal` (`src/ui/SessionDetailModal.tsx`, `src/styles/masthead.css`) | open, close button, backdrop click, Escape, Tab trap, mobile, reduced motion | modal, overlay | `06-modal.md` installed because this is a centered overlay surface | Preserved `t-modal`; tuned close to 220ms; delayed close cleanup until after paint; added synchronized backdrop opacity close transition; kept focus trap and 40px close control | `npm test -- src/ui`; `npm run build`; in-app Browser `iab` on `http://127.0.0.1:5176` verified dialog opens as `.t-modal.is-open` with role `dialog`, `aria-modal=true`, transform/opacity transition at 250ms, backdrop opacity transition, close token `220ms`, and cleanup to 0 modal/backdrop nodes. Close-state class was too transient for the click API to sample but code path is paint-delayed and token-bound. | 90 pass | passing | None |
| `SessionRail` (`src/ui/SessionRail.tsx`, `src/styles/masthead.css`) | default, selected, empty, mobile, reduced motion | navigation, list | no transition fit beyond simple hover/press state; dense rail navigation should not use a heavier reveal | Added exact-property hover/press transitions, 0.96 active scale, and reduced-motion transform guard; existing selected state and 72px mobile minimum remain intact | `npm test -- src/ui/__tests__/sessionRail.test.tsx src/ui/__tests__/controlDesktopShell.test.tsx src/ui/__tests__/operationsPanel.test.tsx`; `npm run build`; route not mounted in active app, so rendered verification unavailable without a harness | 91 pass | passing | None |
| `ConnectionStatus` (`src/ui/ConnectionStatus.tsx`, `src/styles/masthead.css`) | connecting, offline, live, demo, action button, mobile, reduced motion | composite, status banner | no transition fit; status copy/metrics change in place but not frequently enough for text-swap animation | Raised shared `ghost-pill` action to 40px min height; added exact-property transition, 0.96 active scale, and reduced-motion guard via shared pill primitive | `npm test -- src/ui/__tests__/sessionRail.test.tsx src/ui/__tests__/controlDesktopShell.test.tsx src/ui/__tests__/operationsPanel.test.tsx`; `npm run build`; `ConnectionStatus` is not mounted in current `App` beyond type import | 90 pass | passing | None |
| `ControlDesktopShell` (`src/ui/ControlDesktopShell.tsx`, `src/styles/masthead.css`) | desktop shell, sticky rails, responsive | shell, layout | no transition fit; static landmark/layout shell | Verified existing three-pane landmarks, sticky rails, single-column mobile collapse, and no extra motion needed | `npm test -- src/ui/__tests__/controlDesktopShell.test.tsx`; `npm run build`; shell is not mounted in current app route | 90 pass | passing | None |
| `ObservabilityConsoleShell` (`src/ui/ObservabilityConsoleShell.tsx`, `src/styles/masthead.css`) | desktop shell, responsive/mobile | shell, layout | no transition fit; static landmark shell | Existing mounted two-column shell, 1320px rail collapse, and 760px single-column/mobile behavior verified through styles and shell landmarks | `npm test -- src/ui/__tests__/observabilityConsoleShell.test.tsx`; `npm run build`; Browser `iab` exercised mounted shell during sidebar/right-rail checks | 92 pass | passing | None |
| `ObservabilitySidebar` (`src/ui/ObservabilitySidebar.tsx`, `src/styles/masthead.css`) | brand, links, active state, mobile, reduced motion | navigation | no transition fit beyond hover/press states; nav should stay immediate and dense | Raised nav links to 40px hit areas; added 0.96 press scale with exact transition properties; added pure-white dark-mode outline to brand image; covered active transform in reduced motion | `npm test -- src/ui/__tests__/observabilitySidebar.test.tsx`; `npm run build`; Browser `iab` measured six desktop/mobile nav links at 40px, no 390px overflow, image outline `rgba(255,255,255,0.1)` | 95 pass | passing | None |
| `BoardSummary` (`src/ui/BoardSummary.tsx`, `src/styles/masthead.css`) | metric summary, responsive | composite, status | no transition fit; static counters in this component are not animated updates | Existing tabular numeric treatment and responsive grid verified; no extra motion added | `npm test -- src/ui/__tests__/liveBoard.test.tsx`; `npm run build`; component not mounted in current `App` route | 90 pass | passing | None |
| `BriefingStrip` (`src/ui/BriefingStrip.tsx`, `src/styles/masthead.css`) | normal, attention, conflict, responsive | composite, status | no transition fit; brief text is static per projection | Existing pretty body wrapping and attention border treatment verified; no extra motion added | `npm test -- src/ui/__tests__/liveBoard.test.tsx`; `npm run build`; component-level SSR verifies fallback copy | 91 pass | passing | None |
| `ObservabilityMetricGrid` (`src/ui/ObservabilityMetricGrid.tsx`, `src/styles/masthead.css`) | metric cards, empty/live values, responsive | composite, metrics | no transition fit; static metric grid | Added responsive grid styling with stable min card width and mobile single-column collapse | `npm test -- src/ui/__tests__/observabilityMetricGrid.test.tsx`; `npm run build`; grid is exported/tested but not mounted in current `App` route | 91 pass | passing | None |
| `MetricCard` (`src/ui/MetricCard.tsx`, `src/styles/masthead.css`) | default, trend/icon states, demo badge | card, metrics | no transition fit; static metric card | Added metal surface styling, stable 124px min height, tabular value typography, tone colors, and demo badge treatment | `npm test -- src/ui/__tests__/metricCard.test.tsx src/ui/__tests__/observabilityMetricGrid.test.tsx`; `npm run build`; component exported/tested but not mounted in current `App` route | 92 pass | passing | None |
| `ObservabilityRightRail` (`src/ui/ObservabilityRightRail.tsx`, `src/styles/masthead.css`) | telemetry cards, model link, sparkline, mix bar, mobile | composite, rail | no transition fit beyond existing shared card hover and link press; sparkline is static telemetry | Raised model link to 40px hit area; added exact color/transform transition and 0.96 press scale with reduced-motion guard; preserved shared metal card hover | `npm test -- src/ui/__tests__/observabilityRightRail.test.tsx`; `npm run build`; Browser `iab` fixture measured model link 40px and no 390px rail-card overflow | 94 pass | passing | None |
| `ObservabilityStatusBanner` (`src/ui/ObservabilityStatusBanner.tsx`, `src/styles/masthead.css`) | system status, health token, responsive | composite, banner | no transition fit; static status banner | Added metal surface styling, pretty text wrapping, stable health token, and responsive column layout | `npm test -- src/ui/__tests__/observabilityStatusBanner.test.tsx`; `npm run build`; component exported/tested but not mounted in current `App` route | 91 pass | passing | None |
| `SessionBoard` (`src/ui/SessionBoard.tsx`, `src/styles/masthead.css`) | empty, lanes, grid, compact, mobile, density toggle | page state, list | Manual FLIP transition fits compact/comfortable layout changes because grid columns themselves snap in this browser but card transforms can preserve spatial continuity | Density toggle captures old card rects, commits density with `flushSync`, applies transform-based FLIP, and cleans inline styles after 320ms; grid retains exact gap/height timing and reduced-motion branch skips the fallback | `npm test -- src/ui`; `npm run build`; in-app Browser `iab` on `http://127.0.0.1:5176` verified Last week fixture renders 3 cards, compact toggle changes 516x218 cards to 340x178 cards, `.is-layout-animating` appears on 3 cards with running transform transition, and cleanup removes inline styles | 94 pass | passing | None |
| `SessionInspector` (`src/ui/SessionInspector.tsx`, `src/styles/masthead.css`) | empty, selected, summary, feedback, evidence, actions, mobile | composite, inspector | no transition fit; inspector sections are static details inside the modal transition | Existing modal-surface sections, balanced/pretty copy, tabular facts, responsive summary grids, and shared 40px action buttons verified; no extra motion needed | `npm test -- src/ui/__tests__/sessionInspector.test.tsx src/ui/__tests__/liveBoard.test.tsx`; `npm run build`; Browser modal check unavailable in current projection because no card was openable at verification time | 90 pass | passing | None |
| `AttentionQueue` (`src/ui/AttentionQueue.tsx`, `src/styles/masthead.css`) | empty/list, failed command evidence, overflow | list, status | no transition fit; static evidence list | Existing noninteractive evidence-card surfaces, balanced headings, pretty body copy, and tabular count token verified; no extra motion added | `npm test -- src/ui/__tests__/liveBoard.test.tsx`; `npm run build`; rendered attention queue is projection-dependent | 90 pass | passing | None |
| `HistoryPanel` (`src/ui/HistoryPanel.tsx`, `src/styles/masthead.css`) | search, empty/list, facts, mobile | list, form | no transition fit; search field uses focus border only | Raised shared search input to 40px minimum hit area; existing two-column/one-column responsive history cards and ellipsis facts preserved | `npm test -- src/ui/__tests__/historyPanel.test.tsx`; `npm run build`; component is not mounted in current `App` route | 91 pass | passing | None |
| `OperationsPanel` (`src/ui/OperationsPanel.tsx`, `src/styles/masthead.css`) | idle, busy, confirm prune/delete, exported/error, mobile | form, actions | no transition fit beyond shared button press states | Actions inherit shared 40px pill hit area, exact-property transitions, 0.96 press scale, disabled behavior, and reduced-motion guard | `npm test -- src/ui/__tests__/operationsPanel.test.tsx`; `npm run build`; component is not mounted in current `App` route | 91 pass | passing | None |
| `DemoBadge` (`src/ui/DemoBadge.tsx`, `src/styles/masthead.css`) | default | badge | no transition fit; static metadata badge | Added stable 22px badge styling, monospace label, yellow contrast treatment, nowrap fit, and existing `aria-label` verified | `npm test -- src/ui/__tests__/demoBadge.test.tsx src/ui/__tests__/metricCard.test.tsx`; `npm run build` | 92 pass | passing | None |
| `Icon` (`src/ui/icons/Icon.tsx`) | decorative, labelled, size variants | primitive, icon | no transition fit; icon motion handled at parent controls where needed | Existing fixed size tokens, `currentColor`, `focusable=false`, decorative `aria-hidden`, and labelled `role=img` behavior verified by source inspection and component usages | `npm run build`; Browser mounted controls/sidebar/cards used icon tokens without overflow in measured checks | 91 pass | passing | None |
